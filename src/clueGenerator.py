def createNewClue(originalClue, entry):
    
    # Either We can directly get data of google, wikipedia or something else with simulated annealing algorithm.
    # Or implement a embeded algorithm that generate clues.
    
    return originalClue
